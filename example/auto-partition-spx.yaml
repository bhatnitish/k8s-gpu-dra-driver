apiVersion: v1
kind: Namespace
metadata:
  name: gpu-test

---
## [Beta] Request a full GPU (SPX mode) via auto-partition mode.
## The driver will ensure the GPU is in SPX compute mode with NPS1 memory
## when the pod is scheduled. Requires enableAutoPartition=true in Helm values
## and Kubernetes 1.36+.
apiVersion: resource.k8s.io/v1
kind: ResourceClaim
metadata:
  namespace: gpu-test
  name: spx-full-gpu
spec:
  devices:
    requests:
    - name: gpu
      exactly:
        deviceClassName: gpu.amd.com
        selectors:
        - cel:
            expression: >-
              device.attributes["gpu.amd.com"].computePartition == "spx" &&
              device.attributes["gpu.amd.com"].memoryPartition == "nps1"
        capacity:
          requests:
            partitions: "1"

---
apiVersion: v1
kind: Pod
metadata:
  namespace: gpu-test
  name: spx-full-gpu-pod
  labels:
    app: auto-partition-test
spec:
  containers:
  - name: gpu-workload
    image: docker.io/rocm/pytorch:latest
    command: ["bash", "-c"]
    args: ["export; trap 'exit 0' TERM; sleep 9999 & wait"]
    resources:
      claims:
      - name: gpu
  resourceClaims:
  - name: gpu
    resourceClaimName: spx-full-gpu
