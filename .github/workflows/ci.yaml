name: CI

on:
  pull_request:
    branches:
      - develop
      - main
      - 'release-v*'

jobs:
  lint:
    name: Lint
    uses: bhatnitish/ci-workflows/.github/workflows/go-lint.yaml@main
    with:
      go-version-file: go.mod
      build-deps: "pkg-config libdrm-dev"
      check-target: check

  test:
    name: Test
    uses: bhatnitish/ci-workflows/.github/workflows/go-test.yaml@main
    with:
      go-version-file: go.mod
      build-deps: "pkg-config libdrm-dev"
      test-target: test
